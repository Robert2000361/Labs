apiVersion: v1
kind: Pod
metadata:
  name: monitor-pod
spec:
  containers:
    - name: nginx
      image: nginx

    - name: health-check
      image: busybox
      command:
        - /bin/sh
        - -c
        - "while true; do wget -q -O- http://localhost; echo OK $(date); sleep 10; done"
