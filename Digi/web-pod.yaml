apiVersion: v1
kind: Pod
metadata:
  name: web-pod
  labels:
    app: web
    env: lab
spec:
  containers:
    - name: web-container
      image: nginx:latest
      ports:
        - containerPort: 80
