apiVersion: v1
kind: Service
metadata:
  name: nginx-svc
  namespace: test3
spec:
  type: NodePort
  selector:
    app: nginx
    environment: test
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
